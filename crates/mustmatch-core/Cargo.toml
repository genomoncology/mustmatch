[package]
name = "mustmatch-core"
version = "0.0.2"
edition = "2024"
license = "MIT"

[dependencies]
pulldown-cmark = "0.12"
regex = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
shlex = "1"
similar = "2"
once_cell = "1"
