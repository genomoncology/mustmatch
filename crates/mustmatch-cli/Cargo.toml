[package]
name = "mustmatch-cli"
version = "0.0.4"
edition = "2024"
license = "MIT"

[[bin]]
name = "mustmatch-cli"
path = "src/main.rs"

[dependencies]
mustmatch-core = { path = "../mustmatch-core" }
regex = "1"
serde_json = "1"
tempfile = "3"
toml = "0.8"
